extends:
  - recommended

rules:
  # Redirect endpoints correctly return 3xx, and health probes intentionally have
  # no client-error response. These generic REST rules do not fit those contracts.
  operation-2xx-response: off
  operation-4xx-response: off

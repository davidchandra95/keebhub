# sqlc queries

The infrastructure-only health query keeps sqlc configuration checked in CI. Product queries are added with their vertical slices. Keep hand-written SQL here and generated Go under `internal/generated/db`.
